[package]
name = "vp8-decoder-rs"
version = "0.1.0"
edition = "2021"

[features]
# Thread pool — `CONFIG_MULTITHREAD` in the C build. Off in `vp8_only`.
multithread = []

[dependencies]

[dev-dependencies]
md-5 = "0.10"
