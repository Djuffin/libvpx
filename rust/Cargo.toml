[package]
name = "vp8-decoder-rs"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[features]
# Thread pool — `CONFIG_MULTITHREAD` in the C build.
multithread = []

[dependencies]

[dev-dependencies]
md-5 = "0.10"
criterion = "0.5"

[[bench]]
name = "decode"
harness = false
